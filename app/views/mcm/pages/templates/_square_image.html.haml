.container.p-2.d-flex.h-100{ class: components_page.presenter.positioning_classes }
  - components_page.assets.each do |asset|
    = image_tag asset.attachment.variant(:large).processed.url, class: 'img-fluid'
